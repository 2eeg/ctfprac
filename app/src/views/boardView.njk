<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{title}}</title>
    </head>
    <body>
        <p>Hello {{ID}}! <a href="/logout">logout</a></p>
        <p><a href="/">Go Home</a></p>
        {% include "boardList.njk"%}
        <h1>{{board.title}}</h1>
        <p>{{board.content}}</p>
        {%if isAuthor%}
            <p><a href = "/board/delete/{{board.id}}">Delete</a> <a href = "/board/edit/{{board.id}}">edit</a> </p>
        {% endif %}
    </body>
</html>