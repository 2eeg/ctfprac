<ul>
    {% for board in boards%}
        <li>
        <a href="/board/view/{{board.id}}">{{board.title}}</a> | author: {{board.author}} {% if board.is_private %} | private post! {% endif %}
        </li>
    {% endfor %}
</ul>